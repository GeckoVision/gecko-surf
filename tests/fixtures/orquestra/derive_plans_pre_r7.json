{
 "jupiter/SURFACE": [
  {
   "account": "event_authority",
   "note": "Anchor's event-authority PDA. Orquestra reports `pda: null` for it — the seed is a const in program source, not an IDL field (the #4057 class). Recovered recipe: seeds [\"__event_authority\"].",
   "provenance": "recovered",
   "resolver": null
  }
 ],
 "jupiter/plan_route": [
  {
   "account": "token_program",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "user_transfer_authority",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "user_source_token_account",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "user_destination_token_account",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "destination_token_account",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "destination_mint",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "platform_fee_account",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "event_authority",
   "note": "Anchor's event-authority PDA. Orquestra reports `pda: null` for it — the seed is a const in program source, not an IDL field (the #4057 class). Recovered recipe: seeds [\"__event_authority\"].",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "program",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  }
 ],
 "metadao_ico/SURFACE": [
  {
   "account": "launch",
   "note": "the root PDA (['launch', base_mint]) recovered from v07 source — Orquestra's PDA Finder returns ZERO PDAs for this program (no seeds declared in IDL pda.seeds; the deployed program is v07_launchpad, despite the catalog's 'IDL v8' upload-counter label)",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "launch_signer",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "funding_record",
   "note": "per-funder PDA ['funding_record', launch, funder], recovered from v07 source. fund is `init_if_needed`: the PROGRAM initializes it on the funder's first fund and `payer` pays the rent — no separate init instruction exists, so this is NOT a precondition (unlike pump's associated_user).",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "event_authority",
   "note": "generated by Anchor's #[event_cpi] macro (seed ['__event_authority']) — implicit in source, on no machine surface",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "funder_quote_account",
   "note": "the funder's ASSOCIATED USDC token account (Anchor associated_token::mint = launch.quote_mint, authority = funder) — an ATA recipe under the Associated Token program, the same beyond-surface class as pump's associated_user",
   "provenance": "recovered",
   "resolver": null
  }
 ],
 "metadao_ico/plan_fund": [
  {
   "account": "launch",
   "note": "the root PDA (['launch', base_mint]) recovered from v07 source — Orquestra's PDA Finder returns ZERO PDAs for this program (no seeds declared in IDL pda.seeds; the deployed program is v07_launchpad, despite the catalog's 'IDL v8' upload-counter label)",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "funding_record",
   "note": "per-funder PDA ['funding_record', launch, funder], recovered from v07 source. fund is `init_if_needed`: the PROGRAM initializes it on the funder's first fund and `payer` pays the rent — no separate init instruction exists, so this is NOT a precondition (unlike pump's associated_user).",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "launch_quote_vault",
   "note": "launchQuoteVault is NOT derivable client-side: the program's `has_one = launch_quote_vault` constraint makes the field INSIDE the launch account the single source of truth. Gecko reads it from the launch state (one control-plane getAccountInfo — the same dotted-path class as pump's bonding_curve.creator).",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "funder_quote_account",
   "note": "the funder's ASSOCIATED USDC token account (Anchor associated_token::mint = launch.quote_mint, authority = funder) — an ATA recipe under the Associated Token program, the same beyond-surface class as pump's associated_user",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "event_authority",
   "note": "generated by Anchor's #[event_cpi] macro (seed ['__event_authority']) — implicit in source, on no machine surface",
   "provenance": "recovered",
   "resolver": null
  }
 ],
 "meteora/SURFACE": [
  {
   "account": "lb_pair",
   "note": "the helper-seeded ROOT the IDL drops (#4057), recovered from source (derive_lb_pair_pda2): 4-seed recipe [min(x,y), max(x,y), bin_step, base_factor(u16 LE)]. base_factor is the pool's FEE TIER — caller-supplied, not inferable from the mints; omitting it silently derives the WRONG pool for any post-2024-05 pool",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "reserve",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "oracle",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "bin_array",
   "note": "remaining_accounts the IDL never names: which bin_array PDAs the swap traverses is selected from the pool's LIVE liquidity bitmap at active_id (gecko.meteora_math) — declared in the plan, filled at plan time",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "bin_array_bitmap_extension",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "event_authority",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "user_token",
   "note": "an ATA of the user under the SPL Associated Token program — not a PDA of this program, so it appears on neither the IDL nor the program source; hand-modelled so plans can name the user's token accounts",
   "provenance": "recovered",
   "resolver": null
  }
 ],
 "meteora/plan_swap": [
  {
   "account": "lb_pair",
   "note": "the helper-seeded ROOT the IDL drops (#4057), recovered from source (derive_lb_pair_pda2): 4-seed recipe [min(x,y), max(x,y), bin_step, base_factor(u16 LE)]. base_factor is the pool's FEE TIER — caller-supplied, not inferable from the mints; omitting it silently derives the WRONG pool for any post-2024-05 pool",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "reserve",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "oracle",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "bin_array",
   "note": "remaining_accounts the IDL never names: which bin_array PDAs the swap traverses is selected from the pool's LIVE liquidity bitmap at active_id (gecko.meteora_math) — declared in the plan, filled at plan time",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "event_authority",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "user_token",
   "note": "an ATA of the user under the SPL Associated Token program — not a PDA of this program, so it appears on neither the IDL nor the program source; hand-modelled so plans can name the user's token accounts",
   "provenance": "recovered",
   "resolver": null
  }
 ],
 "ore/SURFACE": [
  {
   "account": "config",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "board",
   "note": "['board'] — a const-seeded singleton (source api/src/consts.rs BOARD_ADDRESS pins the same address the seed derives)",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "treasury",
   "note": "['treasury'] — a const-seeded singleton and the ATA owner the treasuryTokens derivation chains off",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "automation",
   "note": "the seed is an authority ARGUMENT, not the tx signer account: an automation bot signs for a different authority (the exact confusion upstream fixed in ore-cli PR #150); the Orquestra-synthesized IDL binds it to 'signer'",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "miner",
   "note": "['miner', authority] recovered from Steel source (api/src/state/mod.rs miner_pda) — for claimOre the authority IS the signer, because claim_ore both seeds by the signer and asserts miner.authority == signer. deploy/checkpoint are the opposite: a separate authority account seeds the miner (ore-cli PR #150's proof_authority confusion)",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "round",
   "note": "geometry vs state: source recovers the shape (['round', id u64 LE]) but the id's VALUE is board.round_id / miner.round_id read from account data — not a free-choice argument, so it stays an honest resolver",
   "provenance": "flagged",
   "resolver": "seed is board.round_id (deploy/reset) or miner.round_id (checkpoint) read from account data — not a free-choice argument"
  },
  {
   "account": "stake",
   "note": "cross-program: the stake account is CPI'd into the SEPARATE ore-stake program (stakecNP...); the Orquestra-synthesized IDL derives it under ORE's own id — a plausible-but-wrong address",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "recipient",
   "note": "the signer's ORE associated-token account (mint oreoU2P8…, classic SPL Token). NO prelude is needed: claim_ore calls create_associated_token_account itself when recipient is empty, with the signer as payer (source-verified) — the same class as MetaDAO's init_if_needed funding_record. The live surface gives this account neither a pda recipe nor an address.",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "treasury_tokens",
   "note": "the treasury PDA's own ORE ATA — a TWO-STEP derivation (treasury = PDA(['treasury'], ORE), then ATA(treasury, mint)) that no IDL/llms.txt surface expresses; the live Orquestra surface reports pda: null for it.",
   "provenance": "recovered",
   "resolver": null
  }
 ],
 "ore/plan_claim": [
  {
   "account": "board",
   "note": "['board'] — a const-seeded singleton (source api/src/consts.rs BOARD_ADDRESS pins the same address the seed derives)",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "treasury",
   "note": "['treasury'] — a const-seeded singleton and the ATA owner the treasuryTokens derivation chains off",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "miner",
   "note": "['miner', authority] recovered from Steel source (api/src/state/mod.rs miner_pda) — for claimOre the authority IS the signer, because claim_ore both seeds by the signer and asserts miner.authority == signer. deploy/checkpoint are the opposite: a separate authority account seeds the miner (ore-cli PR #150's proof_authority confusion)",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "mint",
   "note": "a pinned CONSTANT (api/src/consts.rs MINT_ADDRESS), not a PDA. The shipped idl.json carries it as an `address`; the live Orquestra instruction surface drops that field, so an agent reading the surface alone has nothing to resolve.",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "recipient",
   "note": "the signer's ORE associated-token account (mint oreoU2P8…, classic SPL Token). NO prelude is needed: claim_ore calls create_associated_token_account itself when recipient is empty, with the signer as payer (source-verified) — the same class as MetaDAO's init_if_needed funding_record. The live surface gives this account neither a pda recipe nor an address.",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "treasury_tokens",
   "note": "the treasury PDA's own ORE ATA — a TWO-STEP derivation (treasury = PDA(['treasury'], ORE), then ATA(treasury, mint)) that no IDL/llms.txt surface expresses; the live Orquestra surface reports pda: null for it.",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "bps",
   "note": "claimOre takes an OPTIONAL `bps` arg in source (ClaimORE { bps: [u8; 8] }, api/src/instruction.rs; omitted data = DENOMINATOR_BPS = 100%), but the shipped idl.json and the live Orquestra surface both declare args: []. POST /instructions/claimOre/build returns data '04' even when bps is supplied (wire-verified) — so a partial-claim ask silently becomes a full claim. Gecko declares the source-true bytes; the landing orchestrator refuses to simulate a bps the builder cannot carry rather than land the wrong semantics.",
   "provenance": "flagged",
   "resolver": null
  },
  {
   "account": "board_meta",
   "note": "`board` must be WRITABLE. The shipped idl.json and the live Orquestra surface both declare isMut: false, and /build emits it read-only — but claim_ore ends with program_log, a self-CPI whose inner `log` instruction takes the board as a WRITABLE signer (api/src/sdk.rs: log() = AccountMeta::new(signer, true), invoked via invoke_signed with the BOARD seed). Simulating the builder's version fails with \"BrcSxdp…'s writable privilege escalated / Cross-program invocation with unauthorized signer or writable account\" AFTER the token transfer has already succeeded — a failure no static surface reveals. sdk::claim_ore marks it AccountMeta::new(board, false) (writable) and every real mainnet claimOre carries it writable. Gecko reconciles the meta from source before simulating.",
   "provenance": "flagged",
   "resolver": null
  },
  {
   "account": "checkpoint",
   "note": "checkpoint — the instruction that MOVES a round's winnings into miner.rewards_ore, i.e. claim's precondition — takes 8 accounts on mainnet (193/193 sampled), including a SEPARATE `authority` (which seeds the miner PDA, not the signer) and `automation`. The live surface declares 6 and documents the miner seed as the signer. Building checkpoint from that surface fails with NotEnoughAccountKeys. Not wired here — declared so a claim plan does not pretend the precondition is free.",
   "provenance": "flagged",
   "resolver": null
  }
 ],
 "pumpfun/SURFACE": [
  {
   "account": "global",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "mint_authority",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "event_authority",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "global_volume_accumulator",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "bonding_curve",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "bonding_curve_v2",
   "note": "hidden account: required post-Apr-2026 but the IDL never names it (it travels only as a remaining_accounts entry mentioned in doc text); seed verified against pump-public-docs + mainnet",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "user_volume_accumulator",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "creator_vault",
   "note": "the surface honestly flags the seed (bonding_curve.creator is runtime data); the @49 read recipe that RESOLVES it came from Anchor-decoding the BondingCurve layout, not from the surface",
   "provenance": "recovered",
   "resolver": "seed is bonding_curve.data.creator — fetch the bonding_curve account + Anchor-decode its creator field; the dotted-path seed a naive IDL->derivation drops (#4057)"
  },
  {
   "account": "associated_bonding_curve",
   "note": "generic ATA parameterization: the IDL binds the owner seed to the literal bonding_curve account; re-parameterized on 'owner' so one recipe serves every ATA derivation",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "associated_user",
   "note": "the buyer's ATA — the IDL carries no pda block for it at all; hand-modelled under the SPL Associated Token program",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "fee_config",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  }
 ],
 "pumpfun/plan_buy": [
  {
   "account": "global",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "fee_config",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "bonding_curve",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "associated_bonding_curve",
   "note": "generic ATA parameterization: the IDL binds the owner seed to the literal bonding_curve account; re-parameterized on 'owner' so one recipe serves every ATA derivation",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "associated_user",
   "note": "the buyer's ATA — the IDL carries no pda block for it at all; hand-modelled under the SPL Associated Token program",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "creator_vault",
   "note": "the surface honestly flags the seed (bonding_curve.creator is runtime data); the @49 read recipe that RESOLVES it came from Anchor-decoding the BondingCurve layout, not from the surface",
   "provenance": "recovered",
   "resolver": "seed is bonding_curve.data.creator — fetch the bonding_curve account + Anchor-decode its creator field; the dotted-path seed a naive IDL->derivation drops (#4057)"
  },
  {
   "account": "bonding_curve_v2",
   "note": "hidden account: required post-Apr-2026 but the IDL never names it (it travels only as a remaining_accounts entry mentioned in doc text); seed verified against pump-public-docs + mainnet",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "event_authority",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "global_volume_accumulator",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "user_volume_accumulator",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "fee_recipient",
   "note": "Global carries BOTH a single `fee_recipient` pubkey field (data offset 41) AND a `fee_recipients` array (data offset 162), populated on mainnet with different valid pubkeys. The live Receipt settled which one `buy` accepts empirically: a REGULAR recipient — Global.fee_recipients[0] @ offset 162. The @41 field is a BUYBACK recipient (passing it as fee_recipient reverted 6062), so earlier guidance pointing at @41 is REFUTED. Gecko still will not hardcode a pubkey that can rotate: resolve via the recipe here, or supply your own valid recipient to /build.",
   "provenance": "flagged",
   "resolver": null
  },
  {
   "account": "buyback_fee_recipient",
   "note": "Post-Apr-2026 program (BREAKING_FEE_RECIPIENT.md): `buy` also requires a BUYBACK fee recipient appended AFTER bonding_curve_v2. The 8 valid pubkeys live in Global.buyback_fee_recipients ([pubkey; 8] @ data offset 741) — read them and append as remaining accounts (Gecko appends all 8; the live Receipt confirms that set passes). Same honest pattern as creator_vault: a read recipe, never a guess.",
   "provenance": "flagged",
   "resolver": null
  }
 ],
 "pumpfun/plan_sell": [
  {
   "account": "global",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "bonding_curve",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "associated_bonding_curve",
   "note": "generic ATA parameterization: the IDL binds the owner seed to the literal bonding_curve account; re-parameterized on 'owner' so one recipe serves every ATA derivation",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "associated_user",
   "note": "the buyer's ATA — the IDL carries no pda block for it at all; hand-modelled under the SPL Associated Token program",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "creator_vault",
   "note": "the surface honestly flags the seed (bonding_curve.creator is runtime data); the @49 read recipe that RESOLVES it came from Anchor-decoding the BondingCurve layout, not from the surface",
   "provenance": "recovered",
   "resolver": "seed is bonding_curve.data.creator — fetch the bonding_curve account + Anchor-decode its creator field; the dotted-path seed a naive IDL->derivation drops (#4057)"
  },
  {
   "account": "bonding_curve_v2",
   "note": "hidden account: required post-Apr-2026 but the IDL never names it (it travels only as a remaining_accounts entry mentioned in doc text); seed verified against pump-public-docs + mainnet",
   "provenance": "recovered",
   "resolver": null
  },
  {
   "account": "user_volume_accumulator",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "event_authority",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "fee_config",
   "note": "",
   "provenance": "extracted",
   "resolver": null
  },
  {
   "account": "fee_recipient",
   "note": "Global carries BOTH a single `fee_recipient` pubkey field (data offset 41) AND a `fee_recipients` array (data offset 162), populated on mainnet with different valid pubkeys. The live Receipt settled which one `buy` accepts empirically: a REGULAR recipient — Global.fee_recipients[0] @ offset 162. The @41 field is a BUYBACK recipient (passing it as fee_recipient reverted 6062), so earlier guidance pointing at @41 is REFUTED. Gecko still will not hardcode a pubkey that can rotate: resolve via the recipe here, or supply your own valid recipient to /build.",
   "provenance": "flagged",
   "resolver": null
  },
  {
   "account": "buyback_fee_recipient",
   "note": "Post-Apr-2026 program (BREAKING_FEE_RECIPIENT.md): `sell` also requires ONE buyback fee recipient appended AFTER bonding_curve_v2 — the published totals (16 non-cashback / 17 cashback) allow exactly one, and every observed mainnet sell carries one (unlike the shipped `buy` path, which appends all 8). The valid pubkeys live in Global.buyback_fee_recipients ([pubkey; 8] @ data offset 741) — read them and append index 0 (deterministic; the SDK randomizes only to spread load). Same honest pattern as creator_vault: a read recipe, never a guess.",
   "provenance": "flagged",
   "resolver": null
  },
  {
   "account": "cashback_shape",
   "note": "`sell` has TWO account shapes and the surface says so only in prose (the doc-comment 'For cashback coins, pass as remaining_accounts: [0] user_volume_accumulator, [1] bonding_curve_v2'). The switch is BondingCurve.is_cashback_coin (bool @ data offset 82, after complete@48 + creator@49..81 + is_mayhem_mode@81) — a state read, not a caller choice: cashback coin → 17 accounts with user_volume_accumulator prepended to the appended set; otherwise → 16. Passing the cashback shape for a non-cashback coin (or vice versa) mis-positions every appended account.",
   "provenance": "flagged",
   "resolver": null
  }
 ]
}
