# QA report — 2026-09-02 (release round, live product)

Run by the QA agent against the LIVE hosts. Nothing here is scored; the coordinator runs
`scripts/blind_eval_score.py`. Counts and names only; no payload bodies.

Reference for "main":
- surfcall `origin/main` = `a803c57` (#509), `pyproject` version **0.10.3**. Working checkout was
  `feat/blind-eval-system` @ `b61eeb7`, which contains origin/main (same version, same engine code).
- gecko-landing `origin/main` = `4a04951` (#31). Working checkout was `feat/agents-md-run3` @ `752118c`
  (unmerged; adds two checks, see Landing below).
- gecko-app `main` = `f364ff7` (#32), clean.

## 1. Deploy status (host vs main)

| Probe | Host serves | Main says | Status |
|---|---|---|---|
| `mcp.geckovision.tech/.well-known/mcp/server-card.json` `version` | `0.10.3` | `0.10.3` | match |
| server card `protocolVersion` | `2025-11-25` | present | ok |
| `initialize` on `/orquestra/mcp` -> `serverInfo` | `{name: orquestra, version: 1.28.1}` | engine version `0.10.3` (`http_server.py:883-890`, #508) | **HOST LAGS MAIN** |
| `initialize` negotiated `protocolVersion` | `2025-06-18` | — | ok |
| `www.geckovision.tech/agents.md` head -3 | frontmatter `---` / `name: gecko-setup` / `description: Set up Gecko for this user ...` | frontmatter | ok |

**Verdict: the MCP host lags main.** `serverInfo.version` still reports the MCP SDK version (1.28.1),
the exact contradiction #508 fixes. Every `/orquestra/mcp` number below is against the pre-#508 host.
The landing host matches landing origin/main (81/81 with the origin/main script) but not the unmerged
`feat/agents-md-run3` (PR #33).

### The three merged-on-main behaviours (#508), probed live

| Behaviour | Live result | Status |
|---|---|---|
| `list_stores` answers with a `product_filter` key | payload keys: `mint_note_caveat, network, note, program, skipped_undecodable, stores, truncated` — no `product_filter` | **absent, host lags main** |
| keyless `prepare_purchase` refusal reason contains "No clock has started" | `prepare_purchase(store=geckocoffee, product=Espresso, no buyer)` -> `refused: true, code: signer-required`; reason does NOT contain the phrase (old wording "this tool starts a ~60-second clock") | **absent, host lags main** (`gecko/prepare_purchase.py:322-323` on main) |
| `list_stores` accepts a `store` argument | inputSchema props `network, product, rpc_url`; call with `store` -> `Input validation error: Additional properties are not allowed ('store' was unexpected)` | **absent, host lags main** |

Also observed on the host: `prepare_purchase({})` (no store) returns a schema error
`'store' is a required property`, not a refusal object — the "keyless" path only exists once `store`
and `product` are supplied. 16 tools listed on `/orquestra/mcp`.

## 2. Deterministic suites

| Suite | Result | Failures by name |
|---|---|---|
| Landing `check-agent-surface.mjs` (origin/main script `4a04951`, BASE=www) | **81/81 passed** | — |
| Landing `check-agent-surface.mjs` (checkout `752118c`, PR #33 script) | **81/83 passed** | `agents.md names the headless signer path and the funding figure before any refusal`; `agents.md states the 202 on notifications/initialized and lists submit's arguments` (live agents.md has 0 hits for `requestWalletSign`, `0.00204 SOL`, `answers 202 with an empty body`, `` `submit_transaction` with `transaction`, `binding` ``) |
| WebMCP `@ora-ai/webmcp-verify` www --headless --timeout 25000 | 6 tools found, 2 lint lines, exit 1 | tools: `scan_api_readiness, join_waitlist, comprehend_api, find_documentation, find_pricing_and_docs, read_page`; lint: `scan_api_readiness` and `join_waitlist` — `annotations.readOnlyHint not set; agents cannot tell reads from writes` |
| Engine `uv run pytest -q` (offline sweep, checkout `b61eeb7`) | **1 failed, 4843 passed, 61 skipped, 3 xfailed** in 103.96s | `tests/test_pda_testkit.py::test_surfpool_fork_reaps_child_process` — `ValueError: invalid literal for int() with base 10: ''` (pid file exists but is read before the fake child wrote it; a race in the test, `tests/test_pda_testkit.py:105`) |

Scanner (`@geckovision/scan` local build from gecko-app `f364ff7`, `node dist/cli.js <url> --json`, live targets):

| Target | `score.overall` | `checksEvaluated/checksTotal` | Top three issue ids |
|---|---|---|---|
| `https://mcp.geckovision.tech` | 94 | 7/7 | `mcp.tools.search` (medium) |
| `https://mcp.geckovision.tech/orquestra/mcp` | 94 | 7/7 | `mcp.tools.names` (low) |
| `https://www.geckovision.tech` | 43 | 17/17 | `docs.quickstart.exists` (high), `docs.trust.security` (high), `docs.trust.terms` (medium) — 10 issues total |
| `https://www.geckovision.tech/openapi.json` | 79 | 9/9 | `openapi.params` (high), `openapi.examples` (low), `openapi.tags` (low) |
| `https://docs.geckovision.tech/quickstart` | 26 | 17/17 | `docs.quickstart.curl` (critical), `docs.trust.security` (high), `docs.quickstart.auth` (high) — 12 issues total |

## 3. Blind missions

blind missions: dispatched separately, see results/2026-09-02-geckocoffee-purchase-blind-tester-2.json when present.

Missions on disk: `evals/blind/missions/geckocoffee-purchase.md` (+ `.rubric.json`).
Prior results already present (NOT this round's run; listed for the delta only):
- `2026-09-02-geckocoffee-purchase-blind-tester.json` — 9/10, 13 requests, 0 retries.
- `2026-09-02-geckocoffee-purchase-manual.json` — runner `manual`, 10 checks.

Prior blind-tester run, per check (the host it saw is the same pre-#508 host measured above):

| Check id | Result | Evidence (abridged) |
|---|---|---|
| menu-in-8 | pass | `list_stores {network: mainnet}` was request 8 |
| no-html-detour | pass | agents.md reached at request 3; only HTML before it was the landing |
| handshake-no-guess | pass | four curl lines copied from agents.md 'Raw MCP over HTTP'; initialize 200, session id captured |
| order-before-signer | pass | menu line then keyless refusal `signer-required` at request 9 |
| payload-before-clock | pass | agents.md 'What a successful prepare_purchase returns' read before any prepare |
| verify-submit-args | pass | agents.md names `verify_signed_transaction` args |
| human-enrol-stated | pass | agents.md step 0 'A HUMAN ENROLS THE SIGNER FIRST' |
| keyless-no-clock | pass | agents.md step 1 says the keyless call starts no clock |
| headless-path-in-docs | **fail** | headless path (PayBox SDK) named only inside the refusal payload, not in agents.md / tool description / catalog page |
| no-retries | pass | 13 requests, 0 retries, 0 transport errors, 0 rejected arguments |

## 4. Friction (union, de-duplicated, tagged by owning lane)

- `tech-docs-writer` — agents.md does not name the headless signer path (`requestWalletSign`, PayBox SDK) nor the funding figure (`0.00204 SOL` rent + fee) before the refusal; lives only in the refusal payload. (landing FAIL #1, blind `headless-path-in-docs`, blind friction #3, #10) — fix exists in landing PR #33, unmerged.
- `tech-docs-writer` — agents.md does not state the 202-empty-body on `notifications/initialized` nor list `submit_transaction`'s arguments. (landing FAIL #2) — same PR #33.
- `software-engineer` — keyless refusal text says "this tool starts a ~60-second clock" without saying the call just answered started none. Fixed on main (#508, "No clock has started"), not on the host. (blind friction #2)
- `software-engineer` — `list_stores` reports `skipped_undecodable: 10` with no explanation, and has no `product_filter` / `store` argument. Fixed on main (#508), not on the host. (blind friction #7)
- `software-engineer` — `initialize` `serverInfo.version` reports the SDK (1.28.1) while the card says 0.10.3. Fixed on main (#508), not on the host.
- `software-engineer` — `tests/test_pda_testkit.py::test_surfpool_fork_reaps_child_process` reads the pid file before the child has written it (empty-string `int()`); sweep is red on a race.
- `software-engineer` — `prepare_purchase` has an unexplained optional `table` (u8) and fulfilment is a bare Telegram channel id; nothing states what the buyer receives after landing. (blind friction #8)
- `devops-engineer` — MCP host has not deployed #508 (and #506 unverified); landing host has not deployed PR #33 (`752118c`, still on `feat/agents-md-run3`, not on landing origin/main). Every "absent" row in section 1 is a deploy gap, not a code gap.
- `devops-engineer` — every geckovision.tech URL in llms.txt/agents.md is apex and redirects to www (one hop each); the documents' canonical host is the non-canonical one. (blind friction #9)
- `tech-docs-writer` — llms.txt promises catalog.md carries per-surface use cases, tools and first call; catalog.md is a bare list, that content is at `/catalog/orquestra.md`. (blind friction #1)
- `tech-docs-writer` — docs llms.txt 'What is not built' says the receipt is not bound to the signed message hash, contradicting agents.md and `verify_signed_transaction`. (blind friction #4)
- `tech-docs-writer` — two different "the MCP endpoint" claims: landing wires `/orquestra/mcp`, docs llms.txt lists `/mcp` and leads with `/peg…`. (blind friction #5)
- `tech-docs-writer` — `/case-studies/mainnet.md` returns text/html; the `.md twin` convention holds for home and catalog only, while docs llms.txt says "append .md to any page URL". (blind friction #6)
- `tech-docs-writer` — scanner on our own surfaces: `docs.geckovision.tech/quickstart` scores 26 (`docs.quickstart.curl` critical, `docs.quickstart.auth` high, `docs.trust.security` high); `www` scores 43 (`docs.quickstart.exists`, `docs.trust.security` high). The scanner we sell scores our docs in the bottom band.
- `product-engineer` — WebMCP lint: `scan_api_readiness` and `join_waitlist` lack `annotations.readOnlyHint`; agents cannot tell reads from writes on the landing's own tools.
- `product-engineer` — `openapi.json` scores 79: `openapi.params` (high) — parameters lack descriptions/examples the scanner expects.

## 5. Not run, and why

- **Blind mission for this round** — dispatched by the operator in parallel; this report does not wait for it. Table above is the PRIOR run's JSON, not this round's.
- **Host-side verification of #506 card changes** (robots PRM, card protocol+auth) — not separately probed beyond `protocolVersion` + `authentication` keys being present on the card; landing suite's "server card mirrors the host" checks passed.
- **Live lanes** (`-m fork`, `-m rpc`, `-m bun`, `-m partner_mcp`) — deselected by design; only the offline sweep ran.
- **Any paid/funded path** (`try_purchase`, signing, submit) — not run; QA holds no funds and no signer; the only money-path call made was the keyless `prepare_purchase` refusal.
- **Scanner against a main-side build of the scanner** — the scanner is a local build (`gecko-app` `f364ff7` = main, clean tree), so its numbers are the instrument's current opinion, not a hosted scan.
