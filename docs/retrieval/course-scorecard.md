# Gecko's lexical arm over a document corpus

Generated by `scripts/course_retrieval_report.py`. Do not hand-edit.

    uv run python scripts/course_retrieval_report.py --course-root <bootcamp clone>

The corpus is the Dev3Pack bootcamp: 109 pages, 486,329
characters, page-id fingerprint `c755235e89783e4b`. The questions and the
baseline are the course's own: 25 dev and
24 held-out labelled questions, the held-out set
written before the coach existed and never tuned against. Metric: hit rate at k over
distinct page ids — the coach's metric, which scores whether the right PAGE was
reached and says nothing about whether the passage answers the question.

| variant | chunks | dev@3 | held-out@3 | pooled@3 | what it changes |
|---|---|---|---|---|---|
| `shipped` | 737 | 18/25 (0.72) | 21/24 (0.88) | 39/49 (0.80) | the arm the API surface ships, paragraph chunks at 800, one page per slot |
| `full_corpus` | 2222 | 11/25 (0.44) | 11/24 (0.46) | 22/49 (0.45) | every document a student has, not just units/en: 66 notebooks and the project/cookbook markdown, quizzes and solutions excluded. This is what the surface SERVES, so it is the row that matters; the units-only rows are here to show what including the rest costs |
| `full_corpus_idf` | 2222 | 11/25 (0.44) | 13/24 (0.54) | 24/49 (0.49) | the full corpus, scoring each matched term by how rare it is instead of counting distinct matches. The indicated fix for the miss diagnosis below: misses are vocabulary breadth, not paraphrase |
| `coverage_50` | 737 | 18/25 (0.72) | 21/24 (0.88) | 39/49 (0.80) | a hit must contain at least half the query's CONTENT terms. Added after the out-of-scope pass rate was measured at 23% against a docstring that claimed refusal by construction: one brushed term (`home`) was returning a page for a question about baking bread. This is what the course surface serves |
| `no_one_per_source` | 737 | 17/25 (0.68) | 21/24 (0.88) | 38/49 (0.78) | the post-rank policy off: one long page may take every slot |
| `intent_gate_on` | 737 | 11/25 (0.44) | 11/24 (0.46) | 22/49 (0.45) | the API surface's intent gate applied to prose: a hit must also match the title/tags/page-id surface |
| `sections` | 1007 | 17/25 (0.68) | 20/24 (0.83) | 37/49 (0.76) | chunk at markdown headings first, and carry the heading path in the title |
| `truncating_chunker` | 737 | 18/25 (0.72) | 21/24 (0.88) | 39/49 (0.80) | the course chunker's own handling of an oversized block: cut it (drops table rows, keeps the header) |
| `chunks_400` | 1381 | 19/25 (0.76) | 20/24 (0.83) | 39/49 (0.80) | half the chunk budget — a SWEEP POINT, not a selection: on 25 dev questions one question is 4 points, so picking the best cell here would be fitting noise |
| `whole_page` | 109 | 17/25 (0.68) | 20/24 (0.83) | 37/49 (0.76) | no chunking at all: one page, one unit — the control that says what chunking bought |
| **coach baseline** | 879 | 20/25 (0.80) | 20/24 (0.83) | 40/49 (0.82) | BM25 + title_weight + one_per_page, top_k=3 |

## The result: the shipped arm LOSES on dev, wins on held-out, loses pooled

18/25 against the baseline's
20/25 on dev — three questions worse.
21/24 against
20/24 held-out — one question better.
Pooled over all 49: 39/49 against
40/49. One question is 4 points on dev
and 2 pooled, so the honest reading is: **roughly level, slightly behind, and behind
on the half of the data that was available to tune against.** Gecko does not bring a
better ranker to this corpus. What it brings is refusal, the archetype split, and a
chunker that does not cut tables.

The dev misses say what the gap is, and it is measurable rather than a hypothesis.
The overlap scorer counts DISTINCT matched terms — no term frequency, no IDF, no
length normalisation — so the unit with the broadest vocabulary has the most chances
to overlap any question. The chunks it returns on a miss are bigger than the corpus
average in both dimensions:

| chunk population | mean chars | mean distinct words |
|---|---|---|
| the whole corpus | 658.1 | 73.4 |
| retrieved on a dev miss | 776.5 | 83.0 |
| the gold page it beat | 680.4 | 75.9 |

That is the exact failure the course's own BM25 docstring names ("a page that
mentions everything once stops outranking the page that is about the question"), and
it is the mirror image of `docs/retrieval/README.md`, where our BM25F LOSES to
overlap on API surfaces. Short curated fields favour overlap; long prose favours
BM25. Neither arm is better in general — each is fitted to a field-length
distribution, and nobody had written that down.

### The dev misses, in full

- *what does bootcamp start do* — wanted `unit0/get-settled`, got `bonus/b06-improve-the-coach/introduction`, `tracks/coding-assistant/introduction`, `tracks/demos/introduction`
- *how much does a key cost* — wanted `unit0/local-model` or `unit0/runtime-lanes`, got `unit2/session-07-grounding-metrics/concepts-2`, `tracks/demos/introduction`, `tracks/depth/introduction`
- *how do I set up ollama* — wanted `unit0/local-model`, got `tracks/demos/introduction`, `tracks/first-contribution/introduction`, `unit0/ask-your-assistant`
- *which sessions need a coding assistant* — wanted `unit0/runtime-lanes`, got `tracks/coding-assistant/introduction`, `unit0/ask-your-assistant`, `unit1/session-01-assistant-configuration/concepts-2`
- *what happens on demo day* — wanted `unit0/week3`, got `tracks/demos/introduction`, `tracks/ship-it/introduction`, `tracks/coding-assistant/introduction`
- *how is the course marked* — wanted `unit0/introduction` or `unit0/how-to-submit`, got `tracks/coding-assistant/introduction`, `tracks/cookbook/introduction`, `tracks/final-assignment/introduction`
- *what is AGENTS.md for* — wanted `unit1/session-01-assistant-configuration/concepts-1` or `unit0/runtime-lanes` or `tracks/coding-assistant/introduction`, got `unit0/w12-dsa-for-agents/introduction`, `unit1/session-05-deterministic-mini-agent/introduction`, `bonus/b03-multi-agent-orchestration/introduction`

## Three policy findings, measured rather than argued

1. **The intent gate must be OFF for prose.** Turning the API surface's gate on
   collapses the corpus to
   22/49
   pooled. A lesson does not restate in its heading what it teaches in its body.
2. **Chunking is worth little here, and one-per-page is worth about one question.**
   No chunking at all (`whole_page`) scores
   37/49;
   the shipped 800-character packer scores
   39/49. Section chunking scores
   37/49
   and is not selected.
3. **The truncating chunker scores IDENTICALLY** — which is a finding about the
   METRIC, not a licence to truncate. Cutting an oversized table cannot change which
   PAGE is reached, so a page-level hit rate is blind to it by construction. The
   course's packer drops ~34k characters of tables and lists this way, and no number
   on either side of this comparison can see it. An answer-containment label is the
   measurement that would.

## What is NOT measured here

**Refusal.** Neither labelled set contains an out-of-scope question, so the one
metric surfcall could most usefully hand the course — out-of-scope pass rate — has
no data on either side. `DocIndex` refuses by construction (it turns off the
never-empty prior) and `tests/test_doccorpus.py` pins that, but a property is not a
number.

**Anything about the dense arm.** The course corpus has no paraphrase archetype: a
question that shares no token with its page does not occur here. A dense win on this
corpus would not transfer to `paraphrase_no_overlap`, and a loss would not refute it.
